# BOXINVOICE

Basic Karaoke system with main object to "bajar pepita"

## How to use
Follow these steps in your terminal to get the environment up and running:

```bash
cd bd/
npm run docker:up 
# Or if using an older Windows Docker Compose version:
# npm run docker/w:up
npm run docker:init-db
```