import { Routes } from '@angular/router';
import { Wheel } from './features/karaoke/wheel/wheel.component';

export const routes: Routes = [
    {
        title:"Home",
        path:'',
        component: Wheel
    }
];
